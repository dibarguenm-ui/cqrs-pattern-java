# Configuración MySQL para Query
spring.datasource.url=jdbc:mysql://localhost:3306/bd_query?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=zion120925

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Kafka Configuration
spring.kafka.bootstrap-servers=localhost:9092

# Opcional: Mostrar SQL en logs
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true